#lang info

(define collection "covid-invaders")
(define deps  '("threading"))
(define pkg-desc "Space invaders variation with viruses and vaccines")
(define version "0.1")
(define pkg-authors '(Evan))
